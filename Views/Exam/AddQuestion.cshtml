@* @model AppDev2Project.Models.Question
@{
    ViewData["Title"] = "Add Question";
    Layout = "_DashboardLayout";
}
<div class="container mx-auto px-4 py-8">
    <div class="bg-white shadow-md rounded-lg p-6">
        <h2 class="text-2xl font-bold mb-6">Add Question</h2>

        <form asp-action="AddQuestion" method="post">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="ExamId" />
            
            <div class="mb-4">
                <label asp-for="QuestionText" class="block text-gray-700 font-bold mb-2">Question Text *</label>
                <textarea asp-for="QuestionText" rows="3" class="w-full px-3 py-2 border rounded-lg"></textarea>
                <span asp-validation-for="QuestionText" class="text-red-500"></span>
            </div>

            <div class="mb-6">
                <label class="block text-gray-700 font-bold mb-2">Options</label>
                <div class="space-y-4">
                    <div class="flex items-center space-x-3">
                        <input type="radio" id="optionA" name="correctAnswer" value="A" required>
                        <input type="text" name="ChoiceA" class="flex-1 px-3 py-2 border rounded-lg" placeholder="Option A">
                    </div>
                    <div class="flex items-center space-x-3">
                        <input type="radio" id="optionB" name="correctAnswer" value="B">
                        <input type="text" name="ChoiceB" class="flex-1 px-3 py-2 border rounded-lg" placeholder="Option B">
                    </div>
                    <div class="flex items-center space-x-3">
                        <input type="radio" id="optionC" name="correctAnswer" value="C">
                        <input type="text" name="ChoiceC" class="flex-1 px-3 py-2 border rounded-lg" placeholder="Option C">
                    </div>
                    <div class="flex items-center space-x-3">
                        <input type="radio" id="optionD" name="correctAnswer" value="D">
                        <input type="text" name="ChoiceD" class="flex-1 px-3 py-2 border rounded-lg" placeholder="Option D">
                    </div>
                </div>
                <p class="text-sm text-gray-500 mt-2">Select the radio button next to the correct answer</p>
            </div>

            <div class="mb-4">
                <label asp-for="ScoreWeight" class="block text-gray-700 font-bold mb-2">Score Weight *</label>
                <input asp-for="ScoreWeight" type="number" value="1" min="1" class="w-full px-3 py-2 border rounded-lg" />
                <span asp-validation-for="ScoreWeight" class="text-red-500"></span>
            </div>

            <div class="flex justify-between">
                <button type="submit" class="bg-teal-500 text-white px-6 py-2 rounded-md hover:bg-teal-600">
                    Add Question
                </button>
                <a asp-action="Edit" asp-route-id="@Model.ExamId" class="text-gray-600 hover:text-gray-800">Cancel</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
} *@
